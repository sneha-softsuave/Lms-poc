# ── Web / API ────────────────────────────────────────────────────────────────
fastapi>=0.115.0,!=0.136.3
uvicorn[standard]==0.27.0
python-multipart>=0.0.26
slowapi>=0.1.7

# ── DB / ORM (PostgreSQL) ─────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.25
asyncpg>=0.29.0
alembic==1.13.1
aiosqlite>=0.22.0            # async SQLite — tests only

# ── Config / validation ───────────────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
pydantic[email]
python-dotenv>=1.2.2

# ── Auth ──────────────────────────────────────────────────────────────────────
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# ── AI / RAG (behind the model gateway) ───────────────────────────────────────
anthropic>=0.49.0
openai>=1.50.0
qdrant-client>=1.11.0
sentence-transformers>=3.2.0
tiktoken>=0.7.0
langchain-text-splitters>=0.3.0

# ── Object storage (MinIO / S3 API) ───────────────────────────────────────────
boto3>=1.34.24

# ── Content extraction / OCR ──────────────────────────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23         # NEW vs ThinkArguments — PPTX ingest
pytesseract>=0.3.10
pdf2image>=1.16.3
Pillow>=10.0.0
# Heavier OCR engines (easyocr, docling) are optional; enable in the air-gap phase.

# ── Test ──────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# ── Lint / format ─────────────────────────────────────────────────────────────
ruff>=0.15.0
